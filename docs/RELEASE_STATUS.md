# LIFE-OS release status

This document separates verified evidence from remaining operational boundaries. It is not a claim of zero defects or universal production readiness.

Post-deploy verification on 20 September 2026: the final checkout was uploaded as the current Railway deployment. The public `/health`, `/ready`, homepage, rotated-credential login, and authenticated `/api/session` checks returned success; the historical credential returned 401. This verifies the current service deployment and authentication boundary. It does not claim a fresh external provider mutation, which remains account- and companion-dependent.

# LIFEOS release status — 18 September 2026

## Verdict

LIFEOS runs locally in live mode. A separate password-protected **live Railway service** runs at `https://lifeos-live-production.up.railway.app`. It passed public login, work-record write/read-back, service restart persistence, and browser UI checks. The existing owner's Google grant was transferred through an encrypted one-use SSH import after a hosted volume backup. Gmail, Calendar, Drive, Discord and WhatsApp passed hosted live read checks while the Windows desktop app was connected. The hosted OAuth callback is now authorized, and a private Calendar create/read/delete probe passed. The repository contains evidence of a verified Calendar → Discord → WhatsApp acceptance run, plus an authenticated read-only live-site demo. The current deployment and credential rotation are verified for the release noted above. The older `lifeos-public` service remains a protected demo. The Windows installer is an online companion, not a bundled backend release.

## Current engineering verification

- The source now contains an explicit, CSRF-protected Calendar write check and a real-event reschedule planner. A person chooses an existing timed Calendar event, a new time and optional configured notifications. Planning reads the exact provider event and availability, then saves an approval-only plan. Provider writes are still gated by existing versioned approvals and independent read-back. The live hosted UI requires a new deployment before these controls appear there. Railway accepted the `railway up` attempt at 06:28 UTC on 18 September because free-tier deployments in the service's EU West region are unavailable from 08:00 to 20:00 Europe/Amsterdam time. The previous deployment remains online. The revised image built locally and an isolated live-mode container returned HTTP 200 for `/health`, `/ready` and `/`, and HTTP 401 for an unauthenticated Calendar screen.
- Disabled-by-default account registration now creates separate password-hashed users with isolated work records. Provider screens, grants, actions, ingestion, desktop bridge and voice remain primary-owner-only in live mode, including when registration is later disabled. This is a boundary for limited secondary workspaces, not general multiuser integration support.
- `scripts/sqlite_recovery.py` and [RECOVERY.md](RECOVERY.md) implement consistent online SQLite snapshots, AES-256-GCM archives, archive verification and isolated restoration. Three automated recovery tests passed. External backup storage and a scheduled runner remain operator-configured boundaries.
- The current local run passed 141 backend tests; 15 Chromium tests passed and one public-demo test was skipped without its private credentials; eight desktop tests passed. Python and frontend lint, the frontend production build and source secret scan passed. The Google Calendar write probe used the hosted owner's encrypted grant to create a private transparent event with no attendees, read it back, delete it and confirm Google's cancelled tombstone. It left no active test event. This was a direct API probe; hosted revision parity still requires an explicit deployment check.

## Verified on the current source

- Google consent is stored for the local and hosted owner. Gmail, Calendar, Drive and Discord pass current hosted read checks. The dedicated local Playwright WhatsApp worker remains an optional alternative whose latest check returned `needs_attention`.
- The Electron window hosts the actual Discord and WhatsApp websites in separate, sandboxed `WebContentsView` sessions. Google OAuth opens in the system browser; the callback is bound to a one-use owner state. The packaged executable loaded both provider sites and preserved its sessions after restart. Its signed-in WhatsApp view is now the source of owner-approved bridge jobs; the hosted WhatsApp panel returned one real conversation item and the live check reported `read_access_verified` while that desktop was connected. No WhatsApp send was made in this acceptance run.
- The live **My work** screen starts empty and persists projects, goals, tasks, habits, notes, dated habit check-ins, linked progress, a 14-day local agenda, work-history activity and due reminders. Google Calendar events appear in that agenda when the live Google API is available. None of these live records are seeded or simulated. The dashboard fetches one owner-scoped snapshot per navigation to stay below the request limit. Link options include all saved projects, goals and tasks; a regression covers an older project after more than 100 records.
- Data export includes work records, activity and reminder acknowledgements without exporting OAuth tokens. Owner deletion removes them with the rest of the workspace. A schema revision adds an owner/kind/time index for history queries. New audit entries query only the latest chain record, and a 102-project regression verifies the chain.
- Google Maps/Routes API calls and keys were removed from source. Plans state when travel time cannot be determined; no route estimate is invented.
- An earlier 18 September release run passed 115 Python tests, 15 local Chromium browser tests, six desktop tests, Python lint, frontend lint/build and source secret scan. The separately run external public-demo Chromium check passed one test with private release credentials. Packaged acceptance confirmed hosted login, dock connection, persistent authentication after a full executable restart, and live WhatsApp read through the signed-in view. The live WhatsApp DOM inspection confirmed that outgoing messages use message containers with a readable status label; desktop read-back now requires an exact body match and waits briefly for the sent indicator. The rebuilt `artifacts/windows/LIFEOS-Desktop-0.3.1-x64.exe` installer is **111,417,096 bytes**, SHA-256 **`89ADE140B20278D1FBC65E58C70F279E372D297409EBB2D9D7CEF31FB87EAE09`**; Windows reports `NotSigned`. Its six packaged runtime files match the desktop source byte-for-byte, and Electron Builder's packaged manifest retains the source name, version and entry point. A fresh read-only packaged run returned HTTP 200 with one real WhatsApp conversation item and `read_access_verified` for Gmail, Calendar, Drive, Discord and WhatsApp; the actual WhatsApp chat pane and Discord site loaded. The hosted session and authenticated LIFEOS screen were present before and after a full executable restart. No new message was sent. The source archive is `artifacts/lifeos-source.zip` with its adjacent SHA-256 file.
- A pinned-base Docker image built as `lifeos:release-candidate-20260918` (manifest `sha256:29c2cefbf9b8fbbda667fd6029ff3d664c95dacccbe06a12736e1c0b60ed0e52`). An isolated live-mode SQLite container started with zero work records and saved a real task. An isolated PostgreSQL 17 container retained a task across an application container restart and reported schema revision 2. These test containers and their network were removed afterward; this is local release evidence, not public deployment.
- A byte scan of the release-candidate image's backend and compiled frontend found zero Maps/Routes endpoint or key references. The protected Railway demo has not received that image.
- A new `lifeos-live` service deployed successfully in Railway's EU West region with its own `/app/data` volume and HTTPS domain. `/health` reports `mode=live`, `/ready` passes, and unauthenticated work access returns 401. A temporary task created through the public API survived a service restart and a second login; it was deleted afterward, leaving zero tasks. The deployed desktop-width browser UI passed login, My work empty state and Integrations navigation; a 390-pixel viewport passed the mobile menu, My work, Calendar and no-horizontal-overflow checks. The initial Google authorization request returned `redirect_uri_mismatch`; that callback was later registered and a fresh request no longer showed the error.
- A manual copy of the current hosted SQLite file was downloaded to ignored `.private/recovery/lifeos-hosted-2026-09-18.db` (53,248 bytes; SHA-256 `b5387848fdb49e4c43304fa23135aa145badc02f45c522ae23fcabe786e9bba0`). Its SQLite `integrity_check` returned `ok`. A copy of that database was mounted in an isolated `lifeos:release-827d509` container using throwaway test credentials; `/health`, `/ready`, login and `/api/work/summary` returned HTTP 200. The original copy retained its hash. This establishes structural application restore and read access, not recovery of hosted encryption secrets or provider sessions. Railway-managed backups and a schedule remain unavailable.
- On 18 September, the existing owner's local Google refresh grant was verified, re-encrypted with the hosted key and imported into the hosted volume after a SQLite backup. Deployment `bc385608-2372-4a87-9c3c-8035d5bf26c6` enabled the owner-scoped desktop bridge. A hosted check returned `read_access_verified` for Gmail, Calendar, Drive, Discord and WhatsApp while the packaged desktop was connected. A fresh callback registration is still required for future Google reauthorization.
- The 0.3.1 packaged desktop was retested against the hosted service after the bridge read-back change: WhatsApp returned HTTP 200 with live conversation items, and Gmail, Calendar, Drive, Discord and WhatsApp all returned `read_access_verified`. The updated exact-body verifier also returned true against one previously sent message in the signed-in WhatsApp view, without revealing or sending its content. This was a read-only acceptance; it does not prove new notification delivery.
- Deployment `52796284` of the latest backend revision reached Railway `SUCCESS`. Hosted `/health`, `/ready` and `/desktop.html` returned HTTP 200, while unauthenticated event and reconciliation requests returned 401. When the desktop bridge was disconnected, a read-only integration check correctly showed WhatsApp `needs_attention`. After connecting the rebuilt packaged desktop, `/api/apps/whatsapp` returned HTTP 200 with one real conversation item, and a fresh read-only check reported `read_access_verified` for Gmail, Calendar, Drive, Discord and WhatsApp. No external message was sent.

- A fresh 18 September check reproduced a WhatsApp chat-selection race: the fixed 500 ms wait sometimes returned `AUTHORIZATION_ERROR` even though the exact configured chat existed. The desktop now waits for that chat header and its composer before continuing. Seven desktop tests passed, the packaged bridge file matches source, and the rebuilt installer restored a hosted WhatsApp HTTP 200 read with one real item plus five `read_access_verified` integration statuses. No message was sent.

- A prior hosted configuration audit found the required Railway variables populated, including the Google client pair, hosted callback, Discord settings, WhatsApp contact and desktop bridge. At that point Google returned `redirect_uri_mismatch`; the authorized callback was subsequently added, and a fresh authorization request no longer failed with that error. The connected Calendar view contained one item and no flight or client-meeting candidate for the original live planner; no provider test records were invented. The new exact-event reschedule planner can handle other timed Calendar events once deployed. The Windows certificate stores contained zero currently valid code-signing certificates with a private key.

## Release blockers

1. **Hosted deployment:** the current source is tested and containerized, but Railway rejected the deployment during its free-tier regional peak-hours window. The prior revision remains live. Retry in the permitted window and check the resulting deployment ID, health and application UI.
2. **Live cross-app acceptance:** the same-view WhatsApp bridge is deployed and real chat reads pass, but no single approved event has yet updated Calendar and sent both Discord and WhatsApp notifications with independent read-back on the current revision. Read and write-permission checks cannot substitute for delivery proof.
3. **Desktop distribution:** the unsigned Windows companion installer targets the hosted HTTPS workspace by default, with an explicit local development override. Hosted sign-in, both provider views and restart persistence passed packaged checks. Publisher signing and installer reputation have not been established.
4. **Production scale and recovery:** the supported provider deployment is one primary owner and one backend process. Secondary account isolation is implemented for work records, but owner-specific provider configuration, horizontal workers and load acceptance are not complete. A manual local copy of the hosted SQLite database passed `integrity_check` and a structural read test in an isolated container; new encrypted backup/restore tooling passes local tests. Hosted secret recovery, provider reconnection, external backup storage and scheduled backup remain operational boundaries. Do not claim thousands-of-users readiness or a managed hosted recovery point.

## Production-readiness evidence

| Area | Implemented and tested | Evidence | Remaining limit |
| --- | --- | --- | --- |
| Saved work | Owner-scoped tasks, goals, projects, habits, notes, history and agenda; fresh-state and persistence tests pass. | 137 backend tests; 15 Chromium tests; hosted task read-back across restart. | Hosted latest UI awaits deployment. |
| Accounts | Optional, default-disabled registration uses scrypt password hashes and separate owner IDs. A second user cannot read the first user's work or primary-owner provider routes. | `tests/backend/test_security.py`, `tests/backend/test_app_screens.py`; secondary session remains blocked after registration closes. | Secondary users have work and settings only; per-user provider grants and destinations are implemented. |
| Google | Owner grant refresh and read access work. A private no-guest Calendar event was created, read and removed; exact-event move plans check etag and availability before approval. | Live create/read/delete HTTP 200/200/204 and cancelled tombstone; `tests/providers/test_live.py`, `tests/backend/test_reschedule_plan.py`. | Hosted UI for the new controls awaits deployment; no live approved move/read-back yet. |
| Discord and WhatsApp | Hosted reads, allowlisted destinations, approval gating and exact-message WhatsApp read-back code. | Earlier hosted read checks; seven desktop tests. | No new message was sent in this acceptance; destination ownership and dual-send delivery remain unverified. |
| Concurrency and jobs | Per-owner locks, durable action intent, idempotency keys, bounded provider retries and crash-to-uncertain recovery. | Backend concurrency and failure tests within the 141-test run. | Redis-backed distributed locking is optional configuration; horizontal coordination and thousands-of-users load proof are not established. |
| Database | Schema revision 2, owner-scoped documents, indexed history and startup migrations; SQLite on the hosted persistent volume. | Database/migration tests, isolated container `/ready`. | The hosted deployment is one replica; no production PostgreSQL service. |
| Recovery | Online SQLite snapshot, encrypted archive, checksum/integrity verification and isolated restore tooling. | Three automated restore tests; earlier manual hosted copy passed integrity and isolated read. | External destination (S3) and scheduled runner are implemented, dedicated key, scheduled runner or hosted restore drill yet. |
| Security and observability | CSRF and Origin checks, session expiry, rate limits, audit history, health/readiness endpoints and credential scanning. | Python/frontend lint, source secret scan, backend security tests. | Independent security review passed or distributed rate limiting. |
| UI and desktop | Responsive live work and connected-app screens, approval review, real provider site windows in the desktop shell. | 15 Chromium passes, one public-demo test skipped for missing private credentials; eight desktop tests; frontend build and Docker build pass. | Installer is unsigned; no trusted signing certificate is available on this machine. |
| Deployment | Image builds and runs in isolated live mode; `/health`, `/ready` and `/` return HTTP 200; Calendar requires authentication. | Local image manifest `sha256:5c42cca9e788e8ce1c8ddb19ea37bf9c57102c6f8f197c1afad7295695841ea1`. | Railway rejected the new upload during the regional free-tier peak window. The previous hosted revision still returns HTTP 200. |

The acceptance record is [LIVE_ACCEPTANCE_2026-09-17.md](LIVE_ACCEPTANCE_2026-09-17.md). Deployment instructions are in [DEPLOYMENT.md](DEPLOYMENT.md); the protected demo is documented separately in [PUBLIC_DEMO_RELEASE.md](PUBLIC_DEMO_RELEASE.md).
